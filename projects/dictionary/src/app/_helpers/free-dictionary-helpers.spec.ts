import { FreeDictionaryHelpers } from './free-dictionary-helpers';

describe('FreeDictionaryHelpers', () => {
  it('should create an instance', () => {
    expect(new FreeDictionaryHelpers()).toBeTruthy();
  });
});
